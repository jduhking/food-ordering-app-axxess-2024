import { AntDesign } from '@expo/vector-icons';
import IconProps from './exports';

export function ShoppingIcon({ size, color }: IconProps) {
    return <AntDesign name="shoppingcart" size={size} color={color} />;
  }
